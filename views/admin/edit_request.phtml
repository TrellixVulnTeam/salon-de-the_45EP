<header>
    <h2>Modifier la requête</h2>
</header>
<section>
    <form action="<?=buildUrl('/admin/request/edit', ['id_request' => $request['id_request']]);?>" method="post">
        <fieldset>
            <div>
                <label for="title">Titre</label>
                <input type="text" id="title" name="title" value="<?=$post['title'];?>">
            </div>
            <div>
                <label for="category">Catégorie</label>
                <select name="category">
                    <?php foreach($status as $statu): ?>
                        <?php $selected = $category['id'] === $post['categoryId'] ? 'selected' : ''; ?>
                        <option value="<?=$category['id'];?>" <?=$selected;?>><?=$category['name'];?></option>
                    <?php endforeach; ?>
                </select>
            </div>
            <div>
                <label for="image">Image</label>
                <input type="text" id="image" name="image" value="<?=$post['image'];?>">
            </div>
            <div>
                <label for="content">Contenu</label>
                <textarea id="content" name="content"><?=$post['content'];?></textarea>
            </div>
        </fieldset>
        <input type="submit" value="Enregistrer">
    </form>

</section>